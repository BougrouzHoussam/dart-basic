void main() {
  List<int> list = [7, 8, 9, 3, 2, 3, 4, 5, 6];
  print(list.join('    '));
  //You are trying to access multiple elements from the list using the syntax list[0,1,2], which is invalid in Dart; instead, you should access each element individually or use a sublist.
  //!there is a methode called join it make all the list in one and what's inside
  //! is the space betwwen elements
}
